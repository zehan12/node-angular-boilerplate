{
    "name": "Food-Order-App",
    "version": "1.0.0",
    "engines": {
        "node": ">=16 <19"
    },
    "scripts": {
        "start": "cd backend && node server.js",
        "prebuild": "cd frontend && npm install --force &&  cd .. && cd backend && npm install",
        "build": "cd frontend &&  npm install --force && cd .. && cd backend && npm install"
    }
}